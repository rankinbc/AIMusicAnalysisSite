# Controller ledger — "first impression" work (user: plan items 1,2,3,5,6,7; then "continue")

Worktree `C:/Users/badmin/projects/spectr-solo`, branch `solo`. Trust this file + `git log` over memory.
Approved plan (source of truth for scope/order): `C:\Users\badmin\.claude\plans\glimmering-foraging-liskov.md`.

## Owner decisions (2026-09-20, via AskUserQuestion — binding)
- Demo model: PER-VISITOR SANDBOX (instant guest account; purged later). NOT a shared/read-only account.
- Demo coach: FULLY LIVE. Safety net I added (flags, live-tunable): `coach_guest_messages=20`, `llm_budget_guest_usd=5` separate spend lane.
- Demo track: DECIDE LATER → track-agnostic snapshot export; `demo_enabled=false` in prod until a snapshot is installed.
- Byline: present SPECTR AS A PRODUCT — no personal "built by" anywhere.
- Source link: YES — engineering page links the public GitHub repo as "open source" + CI badge.
- Guard tests: APPROVED to add exactly two lines to the FRONTEND allowlist `src/routes/__tests__/no-social-surface.test.ts`: `demo.tsx` and `trust.how-its-built.tsx`. NO BFF guard edit. Nothing else in either guard file may change.
- Guest uploads: YES, ONE track per guest (mix only). I added `guest_uploads_max=1` + global fail-closed `guest_analyses_per_hour=10`.
- Item 4 (hero motion) is OUT.

## Standing rules (unchanged from earlier ledgers)
- One IMPLEMENTER at a time in the worktree; read-only / new-doc agents may overlap. Every dispatch names a model. Subagent report FILES are refused by the harness → reports come back in the final message. FOREGROUND test runs only.
- Never stage `features/results/AnalysisCompleteModal.tsx` / `.module.css`. Never touch the main checkout or `master`.
- Gates need 127.0.0.1 env overrides; BFF gates with the stack running use `dotnet test --artifacts-path <scratchpad>/…`. Worker baseline = 5 accepted env failures. Run ALL gates after ANY controller edit.
- Commits end with `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`. Pushing `solo` is pre-approved; check CI after each push.
- Layout/CSS claims are verified by MEASURING in a real browser (jsdom has no layout). Persistence claims are verified with a CLIENT-SIDE round trip, not just a reload.

## Plans
- D — `PRPs/guest-demo-sandbox.md` + `-plan.md` (tasks D1–D11) — being written by a fork.
- P — `PRPs/public-surfaces-polish.md` + `-plan.md` (tasks P1–P10) — being written by a fork.

## Execution order
1. P1, P2, P5 → 2. D1–D7 → 3. D9, D10, D8 → 4. P3 → P4 → P6 → 5. P8, P7, P9, P10, D11 → 6. whole-branch review (most capable model) → ONE fix wave → live pass (desktop + 390 px) → push → CI.
Cut list if short: `.rd-row` override, compose healthcheck, Pixel 7 project, lazy Sentry, D8.

## Log
- 2026-09-20: owner named a demo track: `C:/Users/badmin/Music/Artifact303 - Magnetic Fields.mp3` (22.4 MB MP3, file dated 2016). RIGHTS UNCONFIRMED — Artifact303 is a released goa-trance artist, so unless the owner holds the rights or has permission this is LOCAL-DEV ONLY: fine to analyze locally to build/test the snapshot exporter (D3/D4); must NOT be installed as the prod snapshot or committed. Asked the owner. Ruling: pipeline stays track-agnostic, `demo_enabled=false` in prod until a cleared track is installed — cost if wrong: none (one re-export).
- 2026-09-20: owner confirmed "i have permission" for the Artifact303 track -> CLEARED as the prod demo track (owner's statement; I cannot verify it). Still never commit the audio or a snapshot built from it (repo is PUBLIC); it ships via the storage volume only. Engineering/demo pages should carry a credit line for the artist.
- 2026-09-20: demo track uploaded + analyzed LOCALLY in the showcase account (`showcase@spectr.test`): song `a2fe6c31-e9ce-4f7d-95ed-3f0af4b938c4` ("Artifact303 - Magnetic Fields", genre_hint `classic_trance`), version `eb0ef7bb-e5da-42b1-a137-1fc7b9a0fc59`, job `eca86baf-1d85-4f29-9243-a3042a1968d0` = complete. Before exporting a snapshot (D4) it still needs: triage/verdicts generated (open the report once, LLM via local claude CLI), a short coach conversation, and a generated fix rack. Rename the song to a clean title first.
- 2026-09-20: research (3 Explore) + design (2 Plan) done; plan file written; user said "continue" → treated as approval. Forks dispatched to write the two PRP spec+plan pairs.
- 2026-09-20: both PRP pairs committed (P `69e68dc`, D `cf9b485`). Plans are 1274 / 1148 lines (over the ~900 target: they carry complete test code). Per-plan ledgers: `.superpowers/sdd/public-surfaces-polish-plan/progress.md`, `.superpowers/sdd/guest-demo-sandbox-plan/progress.md`. Task headings are "Task P<n>" / "Task D<n>" → `scripts/task-brief` cannot parse them; cut briefs by line range (plan header through Global Constraints + the task block).

## Pre-flight scan — cross-plan shared files (one implementer at a time, so only semantic conflicts matter)
| File | Tasks | Finding |
|---|---|---|
| `src/lib/analytics.ts` `EventName` union | P3, P4, D8, D9 | each adds names; rule: add only if absent. P3 must not re-add `demo_cta_clicked` if D9 added it. |
| `features/landing/LandingPage.tsx` | P3 (CTA+footer), P5 (sample embed only) | D9 ships `/demo` only — the landing CTA is P3's. No conflict. |
| `src/routes/__tests__/no-social-surface.test.ts` | D9 (`demo.tsx` line), P4 (`trust.how-its-built.tsx` line) | two separate owner-approved one-line edits; nothing else. Reviewer must diff this file every time it changes. |
| `src/main.tsx` | P1 (router defaults, boundary), P7 (lazy Sentry/posthog) | P7 builds on P1's `reportError`; order P1 → P7 holds. |
| `src/components/PublicChrome.tsx` | P2 (PricingLink), P3 (How it's built link) | sequential, no conflict. |
| `src/auth/AuthContext.tsx`, `src/api/fetcher.ts` | D9 only | — |
| `features/anon-analyze/*` | D8 only (I fixed the current-job cache there in 915732d — D8 must write the key the same way) | carry a pointer in the D8 dispatch. |
| `routes/_app.tsx` + `_appLayout.module.css` | D10 (banner, upload-once), P8 (nav breakpoint, CSS only) | order D10 → P8 holds. |
| `infra/Caddyfile` | P4 (short link), P6 (bot path), P9 (assets 404, no-cache) | sequential; P9 runs `caddy validate`. |
| `Endpoints/BillingEndpoints.cs` / `EntitlementService` | P2 only | D uses its own `GuestIdentity.Flag` parser. |
| `README.md` | P7 (fairness claim + dropped packages) | — |
No task contradicts another or the Global Constraints. Rubric-vs-plan check: no task mandates a vacuous test or duplicated logic block that I could find from the task lists and fork reports (plans not read line-by-line by the controller — reviewers carry the rubric).
- 2026-09-20/21: P1, P2, P5 COMPLETE (reviews: P1 APPROVED, P2 APPROVED WITH MINORS → fix round done, P5 APPROVED). Commits 061afd5, 1b856d5 (controller: report recurring chunk errors), 040dc08, 7a15bb9, 4c2a249 (controller: caption links), 6325411. Pushed `solo` @ 6325411 — CI run 35554188496 (check result). Frontend 159 files / 1041 tests; BFF 379.
- INCIDENT 2026-09-21: Docker Desktop engine died mid-P2 (docker-desktop WSL distro Stopped). Fixed WITHOUT a reboot: `docker desktop restart` → launcher. Worth adding to docs/STARTUP.md §6 as a new entry (not done yet).
- Shared Playwright browser is LOGGED OUT on purpose (public-page checks). Showcase creds in memory [[repo-polish-2026-09]] / ledger history.
- D1 dispatched. Open carry-forwards: P3 must fix the 390 px header wrap (3+ nav items); P9 gains "quiet 401/404 on a logged-out landing"; final live pass must do the build-twice stale-tab reload check; D4 prep for the demo track (verdicts/triage, coach conversation, fix rack, clean title) — song a2fe6c31…, version eb0ef7bb…, job eca86baf….
- INCIDENT ROOT CAUSE (2026-09-20 23:3x): Docker engine died THREE times (21:13, 22:31, 23:04). NOT random, NOT another session: the engine had 12.5 h uptime; death #1 came 6 min after I uploaded the 9-minute demo MP3; that version had a 2nd job `b25c2d5d-a400-4adb-901a-cc3f9f47203c` stuck `processing` at phase `Arrangement` = `docker run allin1:latest` (4 GB image, CPU, 1800 s timeout, NO --memory cap — `components/analysis/src/audio_analysis/structure/docker_allin1.py:200`). Re-run after each restart → VM down again. MITIGATION: job marked failed (`structure_detection_killed_docker`), orphaned test messages + 475 dead letters cleared from `dramatiq:analysis-free*`, stack relaunched 23:3x. If Docker stays up from here, hypothesis confirmed. docs/STARTUP.md #2d updated.
  - CONSEQUENCE for the demo: "Magnetic Fields" currently has NO structure/arrangement data (phase1.structure absent). The snapshot (D4) would show arrangement as "not assessed". OPTIONS for the owner: (a) add a memory cap to the allin1 `docker run` + give WSL more RAM via `.wslconfig` and retry once, watching `docker stats`; (b) run structure detection on a GPU (`--gpus all` path exists); (c) use a shorter edit of the track; (d) ship the demo without arrangement. Follow-up task candidate (outside both PRPs): `--memory` cap on the allin1 container so the CONTAINER is OOM-killed, never the VM — this is a production risk too.
  - DO NOT re-upload or re-analyze that track (or any long track) on this machine until the cap exists.
- D2 fix round 1 DONE at 8102acb (anon purge honours shared keys; `_lookup_is_guest` real-SQL test found + fixed a uuid-format mismatch via `db_sync.uid_param`). Worker suite 5 failed (accepted) / 683 passed; ruff clean; mypy baseline.
- D3: implementer was BLOCKED by the outage with code complete; RESUMED to verify + commit.
- CHECKPOINT 2026-09-21 ~00:15: pushed `solo` @ 4abbed0. DONE + reviewed: P1, P2, P5, P7, D1, D2. D3 = implemented + Opus review + fix round aad6073 (scoped re-review running). D4 (exporter) implementing. Docker has stayed up since the structure-detection job for the demo track was failed (root-cause hypothesis holding). Frontend 162 files / 1049 tests; BFF 413; worker 5 accepted failures / 683 passed. Entry bundle 726 → 362 KB (gz 230 → 114), CI-enforced.
  - NEXT: D4 review → D5 (`POST /api/auth/demo`) → D6 (guest guard + quotas + coach cap) → D7 (purge) → D9/D10/D8 (frontend) → P3 → P4 → P6 → P8 → P9 (+ quiet 401/404) → P10 → D11 → whole-branch review (most capable model) → ONE fix wave → live pass (desktop + 390 px, Lighthouse) → push.
  - OWNER DECISIONS STILL OPEN: (1) how to get arrangement/structure data for the 9-minute demo track (memory-capped retry / GPU / shorter edit / ship without) — blocks taking the real snapshot, NOT the code; (2) artist credit line wording for Artifact303 (I will add one unless told otherwise).
  - CARRY-FORWARDS: P3 must fix the 390 px header wrap (3+ nav items, measure heights); spec §6.1 governs D4/D5; `npm audit` = 33 pre-existing transitive vulns (pre-launch look); follow-up task = `--memory` cap on the allin1 `docker run`.
- CHECKPOINT 2026-09-21 ~00:45: HEAD e4bd085 (D4 exporter; NOT yet pushed — last push 4abbed0). In flight: D4 review (sonnet, read-only) + D3 fix round 2 (D3 implementer resumed; BFF writer). AFTER BOTH: push → D5 brief = guest-demo plan lines 488-675 → D6 676-798 → D7 799-840 → D8 841-909 → D9 910-1025 → D10 1026-1088 → D11 1089-end. P-plan remaining: P3 479-531, P4 532-668, P6 669-793, P8 794-891, P9 1076-1173, P10 1174-end. Brief = plan header (D: lines 1-27, P: lines 1-31) + the task block. Dispatch template: see any earlier dispatch in this ledger's sibling progress.md files (model sonnet; Opus for reviews of auth/money/data-loss tasks: D5, D6, D7).
  - Controller context is getting heavy (≈80 %): keep dispatches tight, let the ledgers carry state; after compaction resume from THIS line.
- CHECKPOINT 2026-09-21 ~02:00: pushed `solo` @ fbadd14. COMPLETE: P1, P2, P5, P7, D1, D2, D3, D4. D5 implemented (8b9e596) + Opus review → fix round 1 RUNNING (resumed implementer aa28735…). BFF 448, frontend 1049, worker 683 (+5 accepted). NEXT: D5 scoped re-review → D6 (brief = D-plan lines 676-798; carry: `/api/me/export` must be `.DenyGuest()`, reserved-domain `EndsWith` blind spot, guests get ONE upload + ONE analysis, `guest_analyses_per_hour` fail-closed) → D7 (799-840; carry: seed-failure leaves a `devices` row; guests cannot self-delete) → D9 → D10 → D8 → P3 → P4 → P6 → P8 → P9 → P10 → D11 → final review.
- CHECKPOINT 2026-09-21 ~03:30: pushed `solo` @ 0b63dfe. COMPLETE: P1, P2, P5, P7, D1, D2, D3, D4, D5. D6 implemented (0b63dfe; BFF 497) → OPUS review RUNNING (agent afb7a4c…). D7 implementing (agent a076048…, BASE 0b63dfe). D6's fix round must ALSO add the two parked D5 tests (SeedAsync → null on CREATE → 503 + no leftover row; literal `Set-Cookie` Expires ≤ GuestExpiresAt). One BFF writer at a time: D7 is the writer now → D6 fix round waits for D7.
  - THEN: D7 review (OPUS) → frontend D9 (D-plan 910-1025) → D10 (1026-1088) → D8 (841-909) → P3 (P-plan 479-531; MUST fix the 390 px header wrap) → P4 (532-668) → P6 (669-793) → P8 (794-891) → P9 (1076-1173; + quiet 401/404 on a logged-out landing) → P10 (1174-end) → D11 (D-plan 1089-end) → whole-branch review (most capable model) → ONE fix wave → live pass (desktop + true 390 px = request 351 in the MCP browser; Lighthouse; restart the stack first so BFF/workers run the new code) → push → CI.
  - Dispatch recipe: brief = plan header (D: 1-27 / P: 1-31) + task block → `.superpowers/sdd/<plan>/task-<id>-brief.md`; implementer = sonnet, reviewer = sonnet (OPUS for auth/money/deletion); always say: FOREGROUND only, 127.0.0.1 overrides, `--artifacts-path <scratch>/<id>-artifacts`, skips ≠ pass → `wsl -l -v` → BLOCKED, REAL RED first, stage by path, no push, trailer `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`; review package = `scripts/review-package <plan> <commit>~1 <commit>`.
  - OWNER STILL OWES: arrangement data decision for the 9-min demo track (do NOT re-analyze it until the allin1 `docker run` has a `--memory` cap); Artifact303 credit wording; deploy accounts.
- CHECKPOINT 2026-09-21 ~04:30: pushed `solo` @ e1bc819. COMPLETE: P1, P2, P5, P7, D1-D5. IN FLIGHT: D6 fix round PART A (agent a8bc455…; items 1-4 + migration). QUEUED (one BFF writer at a time): D6 PART B (`task-D6-fix1-brief.md` item 9 + minors + the two parked D5 tests) → D7 fix round 1 (`task-D7-fix1-brief.md`) → scoped re-reviews (sonnet) of D6 + D7 → push → frontend D9, D10, D8 → P3, P4, P6, P8, P9, P10 → D11 → whole-branch review → one fix wave → live pass. Every implementer prompt MUST carry the reading discipline (Grep + ≤60-line Read slices, filtered output, commit per item, the CONTEXT WARNING hook is advisory) and ≤ ~4 items per round.
- CHECKPOINT 2026-09-21 ~06:00: pushed `solo` @ a3961cc. BFF 518/0/0. COMPLETE (implemented + reviewed + fixed): P1, P2, P5, P7, D1-D5. D6 + D7: implemented, Opus-reviewed, fix rounds DONE — only the SCOPED RE-REVIEWS remain. Controller context EXHAUSTED at this point — resume here after compaction: (1) dispatch the two scoped re-reviews; (2) D9 → D10 → D8; (3) P3 → P4 → P6 → P8 → P9 → P10; (4) D11; (5) whole-branch review → one fix wave → restart stack → live pass → push → CI. Use the dispatch recipe + reading discipline recorded above.
