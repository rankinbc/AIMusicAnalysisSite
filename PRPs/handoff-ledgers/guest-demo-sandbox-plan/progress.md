# SDD ledger — plan: PRPs/guest-demo-sandbox-plan.md

Spec: PRPs/guest-demo-sandbox.md. Controller ledger with owner decisions: .superpowers/sdd/first-impression-ledger.md
Briefs are cut by line range (plan header + Global Constraints, then the task block) when scripts/task-brief cannot parse the heading.

## Rulings accepted from the plan-writing fork (2026-09-20)
- Ruling: LLM guest lane is resolved at the TWO `effective_tier` sites (`gateway.py:289`, `streaming.py:150`), not at four actor call sites — covers every current and future purpose with two one-line edits — cost if wrong: a purpose that bypasses the gateway is un-laned (none known).
- Ruling: no `exclude_tier` kwarg; global sum = include_all − `_aggregate_tier_spend("guest")` — ~10 existing tests stub that function with a fixed signature — cost if wrong: one extra query per budget check.
- Ruling: `CoachCapService` reads `users.is_guest` itself (3 call sites untouched).
- Ruling: `demo_enabled` flag is seeded FALSE; `Demo:Enabled` config override = true in dev — prod can never serve the sine-tone demo by accident — cost if wrong: one flag flip at launch.
- Ruling: guest detection = DB tuple OR the signed email suffix `@guest.spectr.invalid` (the `tver:` lookup fails open today) — cost if wrong: none; the suffix is unregisterable.
- Ruling: guests get ONE upload + ONE analysis (owner decision); deletes stay DENIED so the quota can't be reset by delete + re-upload; a failed guest analysis cannot be retried (v1) — cost if wrong: a guest with a failed analysis must register to try again.
- Ruling: quota checks are read-then-insert (two parallel uploads could both pass) — accepted as an abuse dampener, not a hard limit.
- Ruling: guest LLM lane is ADDITIVE (worst case = global cap + $5).
- Ruling: the landing CTA belongs to workstream P3; D9 ships `/demo` + `startDemo()` only.
- Unverified by the fork (implementers must confirm, not assume): `User` mirror lint coverage; JSX anchors in `UnifiedUploadDialog.tsx` (:921, :982, :1092); `CoachGateInline.tsx` internals; `register.tsx` validateSearch tolerating `from`; property names on `UploadResponse`/`CoachCapsDto`; group-level `AddEndpointFilter` wrapping nested groups; prod `ForwardedHeaders:Enabled`; Playwright selectors in D11.

## Log
- Task D1: implementer DISPATCHED (sonnet), BASE 6325411, brief task-D1-brief.md. It may stop ONLY the BFF process if `dotnet ef` hits the exe lock despite env ArtifactsPath — if it reports doing so, RESTART THE STACK (launcher + 127.0.0.1 overrides) before any live check. After D1: restart the stack anyway so the running BFF has the new columns.
- Task D1: implementer reported BLOCKED (not a code problem): Docker engine died AGAIN mid-task (2nd time; WSL VM torn down underneath Docker — `Wsl/Service/0x80072746`; cause not identified, documented as docs/STARTUP.md #2d). Implementer wrote all code, committed nothing, did NOT stop the BFF (env `ArtifactsPath` avoided the exe lock for `dotnet ef` — reuse that trick). Controller recovered (`docker desktop restart` → launcher, which applied both migrations) and closed the blocked steps: SQL check = 3 columns (is_guest bool NOT NULL, guest_expires_at timestamptz NULL, guest_device_id varchar(26) NULL), 2 partial indexes WHERE is_guest, 9 flags with the owner's values (demo_enabled=false, guest_ttl_hours=72, coach_guest_messages=20, llm_budget_guest_usd=5, guest_uploads_max=1, guest_analyses_per_hour=10, demo_guests_per_ip_hourly=5, demo_guests_daily_cap=300, anon_sample_per_hour_global=20). BFF 389 passed / 0 failed (first full run had 1 failure in `AnonAnalysisTests.Anon_Upload_Creates_Songless_Device_Job_And_Is_Pollable`; passes alone 7/7 and on the second full run — FLAKE under the full suite, likely the live analysis worker racing the test's job; note for the final review, not D1's). Shared pytest 36 passed. Committed by controller as a302039 (implementer's files unchanged). Review dispatched (sonnet), package review-6325411..a302039.diff.
- STANDING NOTE for every later dispatch: if Postgres/Redis stop answering mid-task, run `wsl -l -v`; if `docker-desktop` is Stopped, STOP and report BLOCKED immediately (do not burn time on retries) — the controller restarts Docker.
- Task D2: implementer DISPATCHED (sonnet), BASE 8d07099, brief task-D2-brief.md — alongside the read-only D1 reviewer. Workers keep OLD code until the next stack restart (restart before any live guest-coach check). docs/STARTUP.md #2d added (1f4cc8f + 8d07099).
- Task D1: review (sonnet) = Spec ✅ / Quality APPROVED WITH MINORS. Important (plan defect — the brief's test only asserted flag NAMES): fixed by controller — new DB-free theory `Seed_Migration_Pins_The_Value` pins all nine seeded values on the migration's SQL (watched it fail on demo_enabled='true' first). Ruling: pin the MIGRATION, not the DB rows — the rows are live-tunable and the BFF suite runs against the shared dev DB, so a value assertion on the row would break the moment anyone flips a flag locally — cost if wrong: none. Minor (mirror test checks nullable/length, not the SQLAlchemy type class) → parked, low value. Task D1: complete.
- Task D2: implementer DONE at 730e712 (worker pytest 5 failed / 681 passed — the SAME 5 accepted env failures as its pre-change baseline 5/667; +14 tests; ruff clean; mypy 12 errors/4 files = baseline). Signatures: `is_shared_key(key: str | None) -> bool` (retention_actor.py), `resolve_lane(user_id, default) -> str | None`, `GUEST_TIER = "guest"`, `reset_lane_cache()`, `_lookup_is_guest(user_id)` (app/llm/lane.py). Deviations (gate-driven, behaviour-neutral): dropped an unused `import pytest`; `is_shared_key` written with an explicit None guard for mypy. Review dispatched (sonnet).
- Task D3: implementer DISPATCHED (sonnet), brief task-D3-brief.md.
- Task D2: review (sonnet) = Spec ✅ / Quality APPROVED WITH MINORS. Confirmed: guest spend is stamped tier="guest" on every gateway/streaming return path; guest is checked ONLY against the guest ceiling; non-guest global = all − guest, floored at 0; same `LlmBudgetExceeded` path; `0` hard-stops (None-check). 3 Important → acted on:
  1. `_purge_unclaimed_anonymous` deletes without `is_shared_key` (inert today; D8's "use our sample" must COPY, but this is the backstop) → D2 fix round 1.
  2. `Demo:SnapshotKey` is operator-configurable — a key (or any asset key in the snapshot JSON) outside `audio/demo/` would be purged with the first guest → sent to the RUNNING D3 implementer as an added requirement (one `SharedPrefix` constant + reject-to-fallback + tests).
  3. `_lookup_is_guest`'s real SQL is executed by no test (a typo = guest lane silently off forever, fail-open) → D2 fix round 1: one real-session test.
  Minors parked: `test_real_spend_still_trips_the_global_cap` only discriminates paired with its sibling; no key normalisation in `is_shared_key` (sole producers are constants — D3 now validates the BFF side); lane cache full-clears at 2048; retention_actor/gateway/streaming already >500 lines (pre-existing).
- Ruling: D2 fix round (Python, `components/worker/**` only) runs IN PARALLEL with the D3 implementer (C#, `components/bff/**` only) — disjoint trees, disjoint test suites, commits by path — cost if wrong: a git index.lock retry.
- Task D2: fix round 1 DONE at 8102acb → Task D2: complete.
- Task D3: implementer DONE at c1e7fcd (was BLOCKED by the Docker outage with code complete; resumed). BFF 407 passed / 0 failed / 0 SKIPPED. Signatures: `DemoSeedResult(Guid SongId, Guid VersionId, Guid JobId, bool FromSnapshot)`; `DemoSeeder.SeedAsync/FindAsync → Task<DemoSeedResult?>`, `DemoSeeder.DemoSongPrefix = "Demo: "`; `DemoSnapshotFormat.{Id="spectr-demo-snapshot/v1", Prefix="audio/demo/snapshot/", DefaultKey}`; `DemoSnapshotStore.GetAsync(ct) → DemoSnapshotTemplate?`, `.Invalidate()`, `internal SharedPrefix="audio/demo/"`, `IsSharedKey`; `DemoSnapshotTemplate.Materialize() → DemoSnapshotDoc`; config `Demo:SnapshotKey` (empty = disabled; pinned "" in TestSupport). Accepted deviations: `$$$` raw-string delimiter; fallback routing plan uses the REAL snake_case shape `{"specialists_to_run":[],"skip":[],"rationale":…,"estimated_total_tokens":0}` (brief said `{"specialists":[]}` — would have left RoutingPlanDto fields null); `FindAsync.FromSnapshot` inferred from `FilePath != DemoAudioKey` (tested both ways); shared-prefix guard added per D2 review. Review dispatched (OPUS — the seeder runs inside every registration request).
- Pushed solo @ c1e7fcd-ish (see git log) — check CI.
- Task D3: review (OPUS) = Spec ✅ / Quality CHANGES REQUIRED. Registration safety PASSES end to end (every snapshot failure → null → fallback; `ChangeTracker.Clear()` before fallback; template immutable; negative result cached; ULID = 30 chars matching ulid_helpers.py; SharedPrefix matches the worker). 3 Important + 6 Minor → fix round 1 (resumed the D3 implementer; BFF-only, parallel with the frontend P7 implementer):
  1. snapshot with no routingPlan → seeded NULL → paid `run_triage` on GET (the leak D3 exists to close) → coalesce to the fallback plan.
  2. Ruling (DEVIATES from spec §6 "export routingPlan verbatim"): when SEEDING, rewrite `specialists_to_run` to keep only slugs that already have a seeded verdict — `CoachTab.tsx:127-141` auto-POSTs `/verdicts/run/{slug}` for every uncovered entry on first view = N paid LLM calls per guest on page load — cost if wrong: the demo roster omits specialists that never ran; a guest can still start one manually under the D7 cap. UPDATE THE SPEC §6 after the fix lands.
  3. asset keys validated pre-substitution but used post-substitution → capture originals and stamp them; D4 (exporter) must ALSO keep asset keys id-free — carry in the D4 dispatch.
  Minors: 5 s timeout + 4 MB cap on the snapshot read (it runs inside the registration/demo request); fixture uses the REAL routing-plan shape; assert each image key lands in its own column + GenreHint + ChainJson; `FindAsync` orders ASC (first "Demo: " song — matters for the D5 landing); test cleanup; title truncation + null-chain guard.
- HOLD D4 until this fix round lands (D4 produces the format).
- Task D3 fix round 1: DONE at aad6073 — BFF 413 passed / 0 failed / 0 skipped; each of the 3 Important fixes RED-checked by reverting the production line. New `Services/DemoSeedMapping.cs` (79 lines: `EmptyRoutingPlanJson`, `RewriteRoutingPlanForSeed`, `TruncateSongName`); `DemoSeeder.cs` 435, `DemoSnapshot.cs` 355. The mirrored frontend rule: CoachTab auto-runs `plan.specialistsToRun` entries whose `name` is not in the set of specialists with status != idle (= a Verdict row exists for that slug) → server keeps only entries whose `name` ∈ seeded `verdict.Specialist`. Spec §6.1 added (commit after aad6073). Scoped re-review dispatched (sonnet). Pushed.
- Task D4: implementer DISPATCHED (sonnet), brief task-D4-brief.md, in parallel with the scoped D3 re-review (D4 only ADDS an admin endpoint + reads the D3 types).
- Task D3 scoped re-review (sonnet): production code CORRECT for all 9 findings; NO new issues (try/catch + ChangeTracker.Clear intact, template immutable, files < 500). 5 ADDRESSED, 4 PARTIALLY — gaps are test coverage + two one-liners:
  (1) only the explicit-null routingPlan shape is tested (absent key + non-object untested); (2) a NON-ARRAY `specialists_to_run` is passed through unrewritten (can't fire triage, but `VerdictEndpoints`' parser could choke on it → make it `[]`); (7) `FindAsync` has no tie-break on equal `CreatedAt` (add `ThenBy(Id)`); (9) title truncation + null-chain skip untested.
  Ruling: fix round 2 = those two one-liners + four small tests. QUEUED until the D4 implementer leaves `components/bff` (same tree, same files nearby) — cost if wrong: none, all are hardening on operator-supplied input.
- Task D4: implementer DONE at e4bd085 — BFF 418 passed / 0 failed / 0 skipped. `POST /api/admin/demo/snapshot {versionId, reason}` (X-Admin-Key, 404 when unconfigured) → `DemoSnapshotExportResponse(SnapshotKey, Verdicts, Messages, RackPresets, AudioBytes)`; errors 400 invalid_request, 404 version_not_found, 409 snapshot_not_ready / snapshot_leak / snapshot_too_large (~3 MB, added). Assets id-free under the snapshot key's directory: `source<ext>`, `spectrogram.webp`, `waveform.webp`, `peaks.json`, then `snapshot.json` LAST (after leak + size guards; copies deleted on abort). Leak scan = owner Guid (D + N) + email, case-insensitive; needed `JavaScriptEncoder.UnsafeRelaxedJsonEscaping` because default STJ escaping hid the email from the substring scan. FINDING: there is NO R2-backed `IFileStorage` in the codebase — Program.cs registers only `LocalDiskFileStorage`; prod storage = the `/data` volume (matches infra/compose.prod.yml) → the exporter is backend-agnostic but only the local-disk path is tested. Install curl recorded in the implementer report (→ D11 docs). Review dispatched (sonnet).
- Task D3 fix round 2: resumed the D3 implementer (two one-liners + four tests).
- Task D4: review (sonnet) = Spec ❌ / CHANGES REQUIRED — 2 Critical + 5 Important (admin auth, leak-scan-on-final-bytes, streaming copy, ordering, llmCallId/user_state exclusion all CONFIRMED solid). Fix round 1 QUEUED behind D3 round 2 (same BFF tree). Rulings:
  C1 exporter never validates the destination → refuse unless `DemoSnapshotStore.IsSharedKey(snapshotKey)` (409 `snapshot_key_invalid`), before any write.
  C2 in-place overwrite of `source<ext>` + delete-on-abort can pair OLD snapshot.json with NEW audio, or delete the LIVE demo's audio on a routine failed re-export → Ruling: each export writes its assets into a FRESH per-export directory `<dir>/<exportId>/…` (random export id — NOT a source id, so tokenization can't touch it), writes `snapshot.json` LAST at the fixed key, and only THEN best-effort deletes the PREVIOUS export's directory (read from the old snapshot.json); abort deletes only the fresh directory — cost if wrong: one stale asset dir left behind. Spec §6.1 wording "id-free" → "free of SOURCE ids" (controller edits the spec).
  I3 `ValidateReason` like every other admin mutation (400 reason_required). I4 empty `Demo:SnapshotKey` = disabled → refuse (409 `demo_snapshot_disabled`), matching the store. I5 round-trip test must assert CONTENT (fix ops, coach evidence/order, rack chain, image columns, routing plan, remapped ids). I6 test `snapshot_too_large`. I7 free-text PII (song title, preset names, the owner's typed coach questions) is NOT covered by the id/email scan → document a manual review step (spec §6.1 + D11 docs); response should list what free text was exported so the operator can read it.
  Minors: IAsyncLifetime cleanup; directory-level test cleanup.
- Task D3 fix round 2: DONE at 6fc922a — BFF 425 passed / 0 failed / 0 skipped (non-array specialists → `[]`; `FindAsync` ThenBy(Id); parameterised routing-plan shapes; long title; null chain; new `DemoSnapshotHardeningTests.cs` 270 lines; `CleanupAsync` now internal). Second transient flake seen under the full suite: `DeviceClaimTests.Register_With_Device_Cookie_Claims_All_Rows_In_One_Shot` (passes on re-run; shared dev DB). Task D3: COMPLETE.
- Task D4 fix round 1: resumed the D4 implementer (2 Critical + 5 Important + 1 Minor). Spec §6.1 extended (atomic exports, destination checks, FreeText disclosure). Pushed through 6fc922a + the spec commit.
- Task D4 fix round 1: DONE at 3d44090 — BFF 436 passed / 0 failed / 0 skipped. Per-export dir `<dir>/<exportId N-guid>/…`, snapshot.json last, previous export retired after; `ResolveSnapshotKey` (null → default, "" → 409 demo_snapshot_disabled, outside audio/demo/ → 409 snapshot_key_invalid); `ValidateReason`; `FreeText{Title, RackPresetNames, UserMessages}` in the response; content round trip; too-large test. Implementer ADMITTED it did not RED-check each item (wrote handler + tests together) → scoped re-review must judge test power. Files: handler 427 lines, tests 539 (over ~500; precedent exists). Scoped re-review dispatched (sonnet).
- Task D5: implementer DISPATCHED (sonnet), brief task-D5-brief.md, BASE 3d44090. Review will be OPUS (auth).
- Task D4 scoped re-review (sonnet): FINDINGS REMAIN. Addressed: destination check, reason, null-vs-empty key parity with the loader, FreeText (user-role only), IAsyncLifetime cleanup. Still open → fix round 2 (QUEUED behind the D5 implementer, same BFF tree):
  (a) IMPORTANT `ReadPreviousAssetKeysAsync` (:245-271) feeds keys from the OLD snapshot.json straight to `DeleteAsync` with no validation → Ruling: retire a key only if it passes `IsSharedKey` AND starts with the snapshot directory AND is not the snapshot key AND is not under the NEW export dir; test with a tampered old snapshot naming `audio/users/x.wav` and `audio/demo/source.wav` (the fallback tone!) → neither deleted.
  (b) IMPORTANT the try/catch spans PAST the go-live write, so a cancellation / `SaveChangesAsync` failure after snapshot.json is written deletes the NOW-LIVE assets → Ruling: `wentLive` flag; cleanup only when !wentLive; post-go-live failures never delete; test with a storage/DB fake that throws after the snapshot write.
  (c) strengthen tests: fix ops full structural equality (gain_db, q…), rack chain structure + masterBypass, ALL FOUR source ids (song/version/job/analysis, D+N) absent from seeded JSON, too-large leaves NO new objects under the test prefix (list the directory), audit row carries the reason.
- Task D5: implementer DONE at 8b9e596 — BFF 446 passed / 0 failed / 0 skipped. New `Auth/GuestIdentity.cs`, `Auth/AuthSnapshot.cs`, `Endpoints/DemoAuthEndpoints.cs` (129), tests (283, 10 tests). `POST /api/auth/demo` → `DemoStartResponse(AccessToken, AuthedUser User, DemoTarget(SongId, VersionId, JobId) Demo, bool Resumed)`; 503 demo_unavailable / 503 demo_capacity / 429 rate_limited. `AuthedUser` gained trailing `IsGuest=false`; `RefreshTokenService` lifetime overloads; `OnTokenValidated` uses `AuthSnapshot(Version, Banned, IsGuest, GuestExpiresAt)` + email-suffix fallback; `appsettings.Development.json` sets `Demo:Enabled=true`. AuthEndpoints.cs 794, Program.cs 665 (both already over the limit). Implementer ADMITTED tests were written AFTER the code (no RED) and that it briefly clobbered + restored D4's `DTOs/DemoDtos.cs`. Known edge (from the brief's pseudocode): an unexpired guest with no seed falls through to a fresh mint, orphaning the seedless row. Review dispatched (OPUS).
- Task D4 fix round 2: resumed the D4 implementer.
- Task D5: review (OPUS) = Spec ✅ / Quality CHANGES REQUIRED. CONFIRMED SAFE: no path mints a guest without limiter AND cap (the seedless-resume fall-through DOES go back through both); device id only from the HMAC cookie; prod honours X-Forwarded-For correctly (Caddy only published port, ForwardLimit=1) — BUT if `ForwardedHeaders__Enabled` is ever dropped from compose the shared ip arm = global 5/h; prod is OFF by default (no `Demo` node in appsettings.json, flag seeded false); expiry evaluated per request; no email/password-change endpoint exists so a guest cannot escape the suffix (structural, not guarded). Fix round 1 QUEUED behind D4 round 2. Rulings:
  C1 resume is limiter-free AND inserts a refresh row per call = unauthenticated unbounded DB writes → on resume DELETE the guest's prior refresh rows then issue one (≤1 row per guest) + a generous separate `demo_resume` limiter (60/h per device; deny 429, exception 503) — spec D2 "resume never consumes the CREATION limit" still holds — cost if wrong: a visitor with 2 tabs gets one tab's refresh token invalidated (it re-resumes).
  I2 wrap the handler: any throw → 503 `demo_unavailable`; a throw after the guest row is committed (seed/refresh issue) compensates by deleting the row. I3 new migration: partial index `ix_users_guest_created ON users(created_at) WHERE is_guest`. I4 resume lookup `OrderByDescending(CreatedAt)`; on resume-seed failure EXPIRE the orphan (GuestExpiresAt=now) before falling through. I5 `ResetPassword` predicate `&& !u.IsGuest`. I6 explicit guest refusals in Login + ResendVerification (cheap; D6 is not in yet). I8 tests with teeth: real daily cap (seed N, cap N), limiter DENY → 429, tampered device cookie → new guest, refresh of an expired guest → 401, forgot-password for a guest → no auth_tokens row + no send_email enqueue, resume consumes no creation limit, seed-null → 503 with no leftover row, `demo_enabled` flag fallback with config unset, cookie `Expires` asserted.
  I9 Ruling: cap race (read-then-insert) ACCEPTED — the per-IP limiter fronts it; document the tolerance in the handler — cost if wrong: a burst overshoots the daily cap by its concurrency.
  Minors now: `Lazy<string>` for the shared hash; null `guest_expires_at` = EXPIRED (Program.cs:217 + AuthEndpoints.cs:451); ErrorEnvelope on the 401; dispose factories; comment about ForwardedHeaders at the ip arm.
  CARRY to D6: `GET /api/me/export` is guest-callable and returns presigned URLs (must be `.DenyGuest()`); register's reserved-domain check is `EndsWith` only (`x@guest.spectr.invalid.`). CARRY to D7: seed-failure leaves a `devices` row; guests cannot self-delete (purge-only).
- Task D4 fix round 2: DONE at fbadd14 with REAL RED ((a) "a foreign key outside audio/demo/ must never be retired" — old code deleted `audio/users/x.wav`; (b) "the new export's audio object must survive a post-go-live failure"). `IsRetireableAssetKey` (IsSharedKey + under the snapshot dir + not the pointer + not the new export dir); `wentLive` gates cleanup. Round-trip now `JsonNode.DeepEquals` on fix ops + rack chain, all FOUR source ids forbidden, audit Reason asserted. BFF 448 effective (one known flake re-run alone: passed). Handler 466 lines; tests 573 + new safety file 211.
  Ruling: the residual (too-large test cannot LIST leftover objects — `IFileStorage` has no listing API; it checks the live snapshot + its audio byte-for-byte) is ACCEPTED — cost if wrong: an aborted oversized export could leave an orphan object under `audio/demo/snapshot/<id>/` (harmless, purge-protected, overwritten by nothing). Task D4: COMPLETE (final whole-branch review covers it again).
- Task D5 fix round 1: resumed the D5 implementer (C1 + I2-I6, I8 + minors; real RED required). Pushed solo through fbadd14.
- Task D5 fix round 1: DONE at 77c5666 + 27e4f46 — BFF 464 passed / 0 failed / 0 skipped (twice). REAL RED for C1 (5 refresh rows → 1; resume limiter), I2 (seed throw → was 200), I4 (older guest returned; orphan not expired), I5 (guest reset → was 204), I6 (guest login with matching password → was 200; resend enqueued send_email), expired-guest refresh envelope. Several I8 tests passed before the fix = regression teeth only (honestly reported). New `IGuestSeeder` seam (DemoSeeder implements it); `demo_resume` limiter 60/h/device; migration `20260921051555_AddGuestCreatedIndex` (partial index, applied + verified); `[Collection("DemoAuth")]` to stop cross-class races on guest rows. Third flake in the shared-dev-DB family: `AnonAnalysisTests.Anon_Jobs_Current_Carries_Resume_Fields`. AuthEndpoints.cs 812, Program.cs 671, DemoAuthEndpoints.cs 225, DemoSeeder.cs 451. Scoped re-review dispatched (sonnet). Pushed.
- Task D6: implementer DISPATCHED (sonnet), brief task-D6-brief.md, BASE 27e4f46. Review will be OPUS.
- Task D5 scoped re-review (sonnet): C1, I2, I3, I4, I5, I6 + all minors ADDRESSED; NO new issues (refresh delete scoped to `t.UserId == u.Id`, unreachable without a valid HMAC cookie, never 0 rows; compensation only for a row minted THIS request and itself try/caught; OperationCanceled rethrown; Login guard sits AFTER bcrypt verify with the identical 401 body → no enumeration oracle; IGuestSeeder resolves to the same scoped DemoSeeder). I8 PARTIALLY: two tests still missing — (a) `SeedAsync` returning NULL on the CREATE path → 503 + no leftover row (only the throw path is tested); (b) the literal `Set-Cookie` `Expires` ≤ `GuestExpiresAt` (only the DB column is asserted).
  Ruling: PARK both as test-only gaps and fold them into D6's fix round (same test area, same `IGuestSeeder` seam) — cost if wrong: two untested-but-reviewed branches. Task D5: COMPLETE.
- Task D6: implementer DONE at 0b63dfe — BFF 497 passed / 0 failed / 0 skipped. `Auth/GuestGuard.cs` (67): `AllowGuest/AllowGuestUpload/DenyGuest<TBuilder>`, default-deny filter on the /api group, 403 `guest_restricted`. `Services/GuestLimits.cs` (97): `CheckUploadAsync` (in the filter), `CheckAnalysisAsync` (top of `VersionEndpoints.DispatchAnalysisAsync`, guest → analysis-free, global arm fail-closed), `GetStateAsync`; `GET /api/me/guest` (`GuestStateDto`). AllowGuestUpload ×3 (POST /versions/, /uploads/init, /uploads/complete); AllowGuest ×19 (notes, rating, rack draft/presets, viz presets, PATCH version/song, tags, set-current, analyze, specialist run, verdict state, fix-rack, coach messages, compare notes, uploads/abort); DenyGuest ×1 (GET /me/export). Exhaustive `EndpointDataSource` test covered 24 unmarked mutating endpoints; RED proven by removing the filter (18/33 failed). CoachCap guest branch before the credits shortcut. `IsGuestEmail` normalised (trim + trailing dot). Changed one D5 test (resend-verification now 403 from the guard, was 204). Review dispatched (OPUS). Pushed.
- Task D7: implementer DISPATCHED (sonnet), brief task-D7-brief.md, BASE 0b63dfe.
- Task D6: review (OPUS) = Spec ✅ / Quality CHANGES REQUIRED. CONFIRMED: filter wraps every nested /api group; guest marking survives the fail-open auth path; anonymous keyed on metadata; every analysis entry point covered; quota cannot be reset (deletes/stems/als/reference unmarked; key forgery into audio/demo/ impossible); specialists are one-shot per slug (409 when a verdict row exists; dismiss keeps the row); coach cap = 20 LIFETIME across all the guest's analyses; /me/export denied. Fix round 1 QUEUED behind the D7 implementer (one BFF writer). Rulings:
  1 guest-originated `run_specialist`, `generate_fix_rack` and the lazy `run_triage` enqueue → `analysis-free` (consistent with the D5 analysis override). NOTE the reviewer's "starves the coach" premise is from CLAUDE.md; the REAL prod compose runs worker-paid on `coach` ONLY and ONE pool drains both analysis lanes, so this does NOT isolate anything in prod today — real protection = the caps in 2-4. Still do it: cheap and right if the lanes are ever split — cost if wrong: none.
  2 `POST /reports/{id}/fix-rack/` is unbounded per guest → new flag `guest_fix_racks_max` (seed 2) counted per guest (usage event or rack_presets source) + 409 when one is already pending; without it ONE attacker burns the whole $5 guest lane and the demo coach is dead for every later visitor.
  3 upload quota read-then-insert + no limiter on `POST /versions/` (N parallel × 250 MB) → atomic backstop: `IRateLimiter` INCR keyed `guest_upload:{userId}` with limit = `guest_uploads_max`, fail CLOSED, in front of the count.
  4 global analysis arm has no per-IP dimension (two IPs can close the demo for everyone) → new flag `guest_analyses_per_ip_hourly` (seed 2) checked before the global arm (`CheckAnalysisAsync` already receives HttpContext and discards it).
  9 exhaustive test EXCLUDES marked endpoints → add a frozen MARKER-INVENTORY test: the set of (method, pattern, quota) carrying GuestAllowed/GuestDenied must equal a literal list — a wrongly added `.AllowGuest()` becomes a build failure.
  Minors: NULL-safe `FilePath` predicate; dispose factories + clean the WAV/user rows in two tests; a test that a REAL user can upload twice + analyze twice and still routes to the tier queue; PLUS the two parked D5 tests (SeedAsync → null on CREATE → 503 + no leftover row; literal Set-Cookie Expires ≤ GuestExpiresAt). Accepted as-is: `/uploads/init` orphan parts (10/min limiter + retention sweep).
- Task D7: implementer DONE at e1bc819 — BFF 504 passed / 0 failed / 0 skipped. `Services/AccountTeardown.cs` (61; `TearDownAsync(User, auditAction, auditReason, ct)` — pure move out of AccountEndpoints; AccountGdprTests untouched + green). `RetentionSweepScheduler.PurgeExpiredGuestsAsync`: predicate `u.IsGuest && (u.GuestExpiresAt == null || u.GuestExpiresAt < now)`, batches of 200 with a fresh scope each, per-guest try/catch, device row deleted only when no other guest references it AND it is unclaimed. RED proven by stashing the implementation. Accepted deviation: ONE AUDIT ROW PER GUEST (matches account deletion + the brief's own test; my "one summary row" instruction contradicted the brief) — cost: ≤ `demo_guests_daily_cap` audit rows/day. Untested: >200 batch boundary. 4th flake in the shared-DB family: `RetentionSweepSchedulerTests.Warns_Due_Tiers_Once_And_Enqueues_Sweep`. Review dispatched (OPUS). Pushed.
- Task D6 fix round 1: resumed the D6 implementer.
- Task D6 fix round 1: the RESUMED D6 implementer ran out of ITS OWN context and made NO edits. Its useful finding: `usage_events.event_type` has a DB CHECK (`IN ('analysis','coach_message')`) and RackPreset has no pending status → Ruling: bound guest fix racks with the ATOMIC `IRateLimiter` (`guest_fix_rack:{userId}`, limit = `guest_fix_racks_max`, window = guest TTL, fail closed) instead of a usage event — no schema change — cost if wrong: a limiter-Redis flush resets the per-guest count (bounded again by the $5 lane).
  The whole round is written up as `task-D6-fix1-brief.md`; a FRESH implementer was dispatched (sonnet) with a context-budget instruction (commit item by item if running low). LESSON: resumed implementers carry their old context — for a multi-item fix round after a big task, prefer a FRESH implementer + a brief FILE.
- D6 fix round 1: a FRESH implementer ALSO stopped at "93 % context" after only READING (it read several 800-1,455-line files whole; a PostToolUse "CONTEXT WARNING" hook then makes agents stop). No edits. RE-DISPATCHED as PART A (items 1-4 + migration + NULL-safe predicate; agent a8bc455…) with a mandatory READING DISCIPLINE (Grep + Read slices ≤ 60 lines, filtered command output, commit after each item, the hook warning is advisory). PART B (item 9 marker inventory + test clean-ups + the two parked D5 tests) to be dispatched after A.
  DISPATCH RULE FROM NOW ON: every implementer prompt carries the reading discipline + "commit after each item"; keep rounds ≤ ~4 items; give line pointers so agents don't hunt through large files.
- Task D7: review (OPUS) = Spec ❌ / CHANGES REQUIRED. CONFIRMED SAFE: real users cannot be selected; AccountTeardown is a pure move; storage deletion untouched (actor filters audio/demo/). Fix round 1 written up as `task-D7-fix1-brief.md` — DISPATCH IT (fresh sonnet implementer, reading discipline, commit per item) AFTER D6 part A leaves the BFF tree. Rulings: C1 infinite loop (attempted-id exclusion + max 50 batches + stop on zero successes); C2 poisoned DbContext → fresh DI scope PER GUEST; I3 DROP the device-row deletion entirely (it strands an anon visitor's in-flight /analyze job; tiny row; unclaimed devices are sweepable elsewhere); I4 a test enqueues REAL `sweep_retention` + `delete_account_data` to the live worker → recording queue everywhere; I5 test seam (`now` + `onlyUserIds`) so tests never run the real sweep over the shared dev DB; I6 failure-isolation + batch-boundary tests (RED first). Spec D8 to gain "NULL guest_expires_at = expired" + "purge never deletes device rows" (controller edit).
  NOTE: until I4 lands, every full BFF test run hands a real retention sweep to the dev worker.
- D6 fix round 1 PART A: DONE at 79b5e5a — BFF 512 passed / 0 failed / 0 skipped. REAL RED: guest specialist/fix-rack/triage enqueued on analysis-paid (now analysis-free via `GuestLimits.QueueFor`); 3rd guest fix-rack was 202 (now 403; limiter exception 503); 8 parallel guest uploads created 3 versions (now exactly 1); per-IP arm did not exist. Migration `20260921064005_SeedGuestCapFlags` applied (guest_fix_racks_max=2, guest_analyses_per_ip_hourly=2). NULL-safe predicate SKIPPED — `SongVersion.FilePath` is `required string` (dead code). NOTE: the new limiter checks are gated behind `RateLimits:Enabled` like every other limiter site — prod leaves the key unset = ENABLED (verified in the D5 review); dev sets it false. Pushed.
- D7 fix round 1: FRESH implementer dispatched (sonnet) with `task-D7-fix1-brief.md` — goes BEFORE D6 part B (Criticals + a test that reaches the live worker).
- D7 fix round 1: DONE at d45e903 + 691ba9a — C1 (attempted-id exclusion, 50-batch cap, stop on zero successes; REAL RED = the new test HUNG >20 s on the old loop), C2 (fresh DI scope per guest via `PurgeOneGuestAsync`), I3 (device-row deletion removed; test now asserts the row SURVIVES), I4 (recording queue everywhere), I5 (`internal PurgeExpiredGuestsAsync(now, onlyUserIds, ct)`; tests never call RunOnceAsync), I6a/I6b, minors. New `IAccountTeardown` seam; AccountGdprTests untouched + green. BFF 513 passed / 1 failed / 0 skipped.
  ⚠ OPEN: `AnonAnalysisTests.Anon_Upload_Creates_Songless_Device_Job_And_Is_Pollable` now fails even ALONE (`Expected Conflict, Actual OK`, AnonAnalysisTests.cs:128 — the one-active-analysis-per-device 409). Hypothesis: the LIVE analysis worker consumes the first test job instantly (tiny fake file → failed → no longer "active") so the 2nd upload is allowed. NOT yet verified. TO DO before the final review: stop the dev workers and re-run that test alone; if it passes → environment (tests must use the recording queue there too); if it still fails → bisect between 27e4f46 and 691ba9a (suspects: D6 `GuestGuard` filter on the anon endpoint with a guest/anon principal; D6 part A).
- D6 fix round 1 PART B: FRESH implementer dispatched (sonnet). Pushed solo through 691ba9a.
- D6 fix round 1 PART B: DONE at 267b6af + f324ed4 + a3961cc — BFF **518 passed / 0 failed / 0 skipped**. Frozen marker inventory (29 entries; RED proven by temporarily adding `.AllowGuest()` to `DELETE /api/versions/{versionId}`), test clean-ups, real-paid-user-analyzes-twice → `analysis-paid`, the two parked D5 tests (seed-null on CREATE → 503 + no leftover row; literal Set-Cookie Expires ≤ GuestExpiresAt).
  ✅ OPEN ITEM CLOSED: the `AnonAnalysisTests…Is_Pollable` failure was CONFIRMED as the live-worker race (test used the real queue; the dev worker flips the tiny fake job to `failed`, so `HasActiveAnalysisAsync` no longer sees an active job → 200 instead of 409). Fixed by switching that ONE test to the recording queue; 3/3 solo runs green. No bisect needed. (The other three flaky tests in that family likely share the cause → candidate clean-up for the final review.)
- NEXT: scoped re-reviews (sonnet) of D6 (range 0b63dfe..a3961cc minus D7 commits) and D7 (e1bc819..691ba9a) → then frontend D9 (D-plan lines 910-1025). Pushed solo through a3961cc.
