# SDD ledger — plan: PRPs/public-surfaces-polish-plan.md

Spec: PRPs/public-surfaces-polish.md. Controller ledger with owner decisions: .superpowers/sdd/first-impression-ledger.md
Note: task headings are "Task P<n>" so scripts/task-brief cannot parse them — briefs are cut by line range (header lines 1-31 + the task block).

## Log
- Plan + spec committed at 69e68dc (fork-written; plan is 1274 lines — over the ~900 target because it carries complete test code). Fork deviations accepted as rulings:
  - Ruling: engineering page says "checksum-verified scanner", NOT "SHA-pinned actions" — only the Trivy action is pinned (ci.yml:241,248,255) — cost if wrong: none, understates.
  - Ruling: numbers are FLOORS by static count (950+ frontend test cases, 350+ BFF, 25+ specialists) — the runner's 1016 includes parametrised cases the static counter can't see — cost if wrong: page understates.
  - Ruling: `creditsEnabled` is `bool?`; unknown/failed read HIDES Pricing (was: falls back to on) — cost if wrong: Pricing link missing during a flag-read outage while credits are on.
  - Ruling: CI "badge" is a text link, no hot-linked image (would be the site's only unconditional third-party request) — cost if wrong: one line to swap.
  - Ruling: bounded triage wait is timer-based with a client-only `triage_timeout` reason and NO Retry button (the notice component's own comment forbids one).
  - Ruling: no `caddy depends_on bff healthy` (would turn a degraded API into a dead site).
  - Open for P4 implementer: "tool toggles change params, not topology" has no supporting line in useAudioGraph.ts (pitch mode swaps the source node) — confirm or drop the sentence.
- Task P1: implementer DISPATCHED (sonnet), BASE 69e68dc, brief task-P1-brief.md.
- Task P1: implementer DONE at 061afd5 (156 files / 1023 tests; live 404 ok at 1440 + 390; deviation: stale-chunk build-twice manual sim skipped — needs vite preview; unit-tested instead → do it in the final live pass). Review dispatched (sonnet), BASE cf9b485, package review-cf9b485..061afd5.diff.
- Task P2: implementer DISPATCHED (sonnet), BASE 061afd5, brief task-P2-brief.md. Runs in parallel with the read-only P1 reviewer (different files). NOTE for the live pass: the running BFF predates P2, so /api/billing/plans has no creditsEnabled until the stack is restarted — restart before the final live pass.
- Task P1: review (sonnet) = Spec ✅ / Quality APPROVED, no findings. Deviation (no vite-preview stale-tab sim) accepted → final live pass.
  - Ruling (plan defect, reviewer's design note): when a chunk error recurs INSIDE the 5-min reload guard, `RouteErrorScreen` returns early and never calls `reportError` — a persistently broken deploy would go unreported, which is exactly the case worth an alert. Fix = report when `reloadOnceForStaleChunk()` returns false. Controller will apply it (one line + one test) AFTER the P2 implementer finishes (no concurrent builds in the worktree) — cost if wrong: one extra Sentry event per stuck visitor.
- Task P1: complete (pending the controller follow-up above).
- P1 follow-up applied by controller: commit (see git log "a chunk error that recurs inside the reload guard is reported"), test-first, all frontend gates green (1036 tests).
- INCIDENT: Docker Desktop's engine died mid-P2 (docker-desktop WSL distro Stopped, `docker ps` no answer; NOT the zombie-port case — ports were held by a live com.docker.backend). Recovery that worked, no reboot: `docker desktop restart` → launcher `scripts/start-spectr.ps1` with the 127.0.0.1 overrides. Launcher marked 1 stranded `processing` job failed.
- Task P2: implementer DONE_WITH_CONCERNS at 040dc08 (BFF integration tests + live check blocked by the outage). Controller closed both concerns after recovery: BFF suite 379 passed / 0 failed; live `/api/billing/plans` returns `creditsEnabled:false`; `/pricing` = "Free while we launch.", no price grid, 0 `/pricing` links on /pricing and /trust/no-training, no horizontal overflow at 433 px and 1600 px (the MCP browser's effective widths for 390/1440). Credits-ON path verified by tests only. Review dispatched (sonnet), package review-061afd5..040dc08.diff.
- Task P5: implementer DISPATCHED (sonnet), BASE 1b856d5, brief task-P5-brief.md — runs alongside the read-only P2 reviewer. Next after P5 + reviews: push solo + check CI, then D1 (brief = guest-demo plan lines 1-27 + 28-82).
- Task P2: review (sonnet) = Spec ✅ / Quality APPROVED WITH MINORS (no Critical/Important). Minors → fix round 1, to run AFTER the P5 implementer leaves the worktree (resume the P2 implementer):
  1. live credits-ON check via `page.route` stub (brief Step 7) — "Sign in"/"Analyze free" must not shift when the Pricing link appears;
  2. `PublicSiteEndpoints.cs:102-105` comment is stale (the /pricing shell copy now also changes on a live flag flip while `Cache-Control: max-age=3600` stays) — Ruling: fix the COMMENT, keep max-age (crawler-only copy; ≤1 h staleness after a flag flip is harmless) — cost if wrong: a link preview says "free" for up to an hour after credits are switched on;
  3. add a PricingPage-level test for "200 OK but `creditsEnabled` missing" → unavailable state;
  4. rename the stale `describe('PricingPage …')` in landing.test.tsx (it renders PricingPlansView now).
- Task P5: implementer DONE_WITH_CONCERNS at 7a15bb9 (159 files / 1040 tests); could not see the landing (shared browser was logged in; it correctly did not log out). Controller logged the browser OUT (`POST /api/auth/logout` → 204; still logged out — better for public-page checks; showcase creds in memory) and measured in Chrome: desktop (1600 css px) card 992×496, all 3 plain lines + summary + LUFS gloss + /demo link present, 0 `/pricing` links, no overflow; phone (true 390 css px = request 351 in this MCP browser) card 336×1125, nothing past the card edges, no overflow. Defect found + fixed by controller (4c2a249): the two caption links sat 2 px apart and read as one link → " · " separator. All gates green (1040). Review dispatched (sonnet), package review-1b856d5..4c2a249.diff.
  - Implementer's judgment call accepted: reading order = plainSummary → grade → pills → LUFS gloss → findings (brief said "above the list"); looks right on screen.
- FINDING for P9 (hardening): a logged-out first visit logs TWO red console errors on the landing — `POST /api/auth/refresh` 401 and `GET /api/anon/jobs/current` 404. Expected statuses, but a technical visitor with DevTools open sees red. Ruling: add to P9 — both endpoints answer quietly (e.g. 200/204 "no session" / "no job") with the clients updated; carry it in the P9 dispatch — cost if wrong: small BFF+client change nobody notices.
- Task P2 fix round 1: resumed the P2 implementer (4 minors), HEAD 4c2a249.
- Task P5: review (sonnet) = Spec ✅ / Quality APPROVED, no findings (copy byte-identical to D9; pinning test real; 720px media query resets the renumbered grid rows). Task P5: complete.
- Task P2 fix round 1: DONE at 6325411 (comment, missing-flag page test — passed immediately, already-correct behaviour now pinned — describe rename, live credits-ON check). BFF 379/379, frontend 159 files / 1041. Measured: at 1440 "Sign in"/"Analyze free" x-positions byte-identical with/without the Pricing link (D6 holds). Task P2: complete.
  - FINDING → carry into the P3 dispatch: at 390 css px the header nav cannot fit THREE items — text wraps to two lines (Sign in 15.6→31.1 px tall). Invisible today (credits off = 2 items) but P3 adds "How it's built" = 3 items at launch. P3 must make the chrome fit at 390 AND 360 (plan says hide text links ≤480 px) and MEASURE element heights (single-line) live, with credits stubbed on as the worst case (4 items).
- Task P7 (bundle diet): implementer DISPATCHED (sonnet) IN PARALLEL with the D3 review — frontend-only, disjoint from components/bff. brief task-P7-brief.md.
- Task P7: implementer DONE at 1c030f5. Entry chunk 725,970 B raw / 230,461 B gz → 361,983 B raw / 113,743 B gz (budget ≤400,000 / ≤130,000 — met; enforced by new `npm run lint:bundle` = scripts/check-bundle-size.mjs, wired into ci.yml after Build). posthog + Sentry are lazy chunks (no modulepreload); zod, recharts, wavesurfer.js, ai removed (grep-proven); trust pages + kitchen-sink components moved out of route files (no route FILE added/removed — guard untouched); Syne font preloaded; README fairness claim + stack table corrected. vitest 162 files / 1049. Live check on the dev server OK (logged out; only the 2 expected network errors; `/login?next=` survives the hand validator). Deviation accepted: no Lighthouse/`vite preview` run (byte budget measured exactly instead) → Lighthouse stays in the final live pass. Note: `npm audit` = 33 pre-existing transitive vulns (not P7's; look at before launch). Review dispatched (sonnet).
- Task P7: review (sonnet) = Spec ✅ / Quality APPROVED WITH MINORS. It independently re-measured the entry (361,983 / 113,743), proved the four moves byte-identical, the lockfile diff = only the four removed packages, `Sentry.init` options unchanged, validators strip unknown keys like zod did and `safeNext` untouched, budget script reads the entry from dist/index.html and exits 2 (not silent) when dist is missing. Important (no PRODUCTION-build live check — dev server serves unbundled ESM) → CLOSED BY CONTROLLER on `vite preview` :4173 (temporary; stopped again): landing fetches a 111 KB entry + 8 tiny chunks; route chunks (pricing, trust, _public, login, register) load only on navigation; NO posthog/Sentry chunk fetched without keys; `/login?next=%2Flibrary` keeps its param; unknown URL → 404 screen; no overflow; console = only the 2 expected logged-out statuses.
  - P1's deferred stale-tab check DONE on the same preview build: removed `analyze-*.js` from dist, client-navigated to /analyze → page reloaded ONCE (`navType: reload`, `spectr:chunk-reload-at` set) → chunk still missing → "Something went wrong on this page." with Reload / Back to home → same document for 6 s = NO LOOP. Note: TanStack Router keeps its OWN one-shot guard (`tanstack_router_reload:<msg>` in sessionStorage) so worst case is two reloads, never a loop. Chunk restored.
  - Minors: stale "Sentry.ErrorBoundary" comments → fixed by controller (dfbe320, all gates incl. lint:bundle green, 1049 tests). Parked: no test for double-`init` single import; `identifyUser(null)` reset not asserted on the lazy path; non-string `next` now dropped quietly instead of a ZodError (not a widening).
- Task P7: complete. Lighthouse mobile still belongs to the final live pass.
