{
  "Logging": {
    "LogLevel": {
      "Default": "Debug",
      "Microsoft.AspNetCore": "Information"
    }
  },
  "// Keys": "DEV-ONLY signing keys (story 4.1 / NFR6). This file only loads when ASPNETCORE_ENVIRONMENT=Development — production boots REFUSE to start unless Jwt__Key / Anon__SigningKey are supplied via env or user-secrets. Both must be >= 32 bytes.",
  "Jwt": {
    "Key": "spectr-dev-only-signing-key-not-for-production-use"
  },
  "Anon": {
    "SigningKey": "spectr-dev-only-anon-signing-key-not-for-production"
  },
  "// RateLimits": "Story 4.3: auth-endpoint rate limits are OFF in Development — the test suite registers hundreds of accounts per minute from one IP. Every other environment enforces (default on).",
  "RateLimits": {
    "Enabled": "false"
  },
  "// Auth": "Story 12.1: DevAutoVerify stamps EmailVerifiedAt at registration so the story-4.5 second-analysis verify gate is satisfiable without email delivery. Code honors it ONLY when ASPNETCORE_ENVIRONMENT=Development (env check is defense in depth) — never add this key to any other config.",
  "Auth": {
    "DevAutoVerify": "true"
  }
}
