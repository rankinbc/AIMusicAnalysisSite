# Copy to .env.local for local overrides. All values optional.

# "Report a problem" destination address. Falls back to the project owner's
# personal email — acceptable for the private beta, set a real support inbox
# before public exposure.
VITE_SUPPORT_EMAIL=support@example.com
