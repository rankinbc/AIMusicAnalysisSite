// Re-export from AuthContext so feature code can import from a local path.
// TODO: /execute-prp may extend this with per-feature derived selectors.
export { useAuth } from '../../contexts/AuthContext'
