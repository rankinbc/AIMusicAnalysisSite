# Linux / Mac — use prefork pool
# worker: celery -A app.celery_app worker --loglevel=info --concurrency=1 --pool=prefork

# Windows — solo pool required (uncomment the line below instead)
worker: celery -A app.celery_app worker --loglevel=info --concurrency=1 --pool=solo
