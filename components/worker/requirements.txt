aimusic-shared>=0.1.0
# Queue: dramatiq replaced celery in v2 (see app/dramatiq_app.py).
# Celery files are still present for reference but are not loaded by the dramatiq entrypoint.
dramatiq[redis]>=1.16
celery>=5.3.0
redis>=5.0.0
sqlalchemy[asyncio]>=2.0.0
psycopg2-binary
# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
aiosqlite>=0.20.0
