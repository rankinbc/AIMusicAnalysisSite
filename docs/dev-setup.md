# SPECTR dev setup

**Moved.** All startup, restart, environment, and troubleshooting content now
lives in the single canonical guide:

→ **[docs/STARTUP.md](STARTUP.md)**

That file covers: prerequisites and one-time installs, the one-command boot
(`./scripts/start-spectr.ps1` and its flags), manual boot order, how to verify
the stack is actually up, the env-file reality map, and every startup problem
we have previously hit (numbered, with fixes).

Production operations: `docs/runbook.md`. Launch gates: `docs/launch-checklist.md`.
