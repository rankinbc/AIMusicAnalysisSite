# schemas

Shared OpenAPI/JSON schema files consumed by both the API and frontend. The frontend TypeScript types are generated from these.

Generate TypeScript types: `npx openapi-typescript schemas/openapi.json -o components/frontend/src/types/api.d.ts`
